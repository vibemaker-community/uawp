name: Bounded fuzzing

on:
  workflow_dispatch:
  schedule:
    - cron: "17 3 * * 1"
  push:
    branches: [main]

concurrency:
  group: fuzz-${{ github.workflow }}-${{ github.run_id }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  fuzz:
    timeout-minutes: 15
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5
        with:
          go-version-file: go.mod
          cache: true
      - name: Fuzz untrusted inputs with a fixed budget
        run: |
          go test ./internal/e2e -run '^$' -fuzz '^FuzzDecodeManifest$' -fuzztime 5s
          go test ./internal/e2e -run '^$' -fuzz '^FuzzManagedBlockEditing$' -fuzztime 5s
          go test ./internal/e2e -run '^$' -fuzz '^FuzzResolveUAWP$' -fuzztime 5s
          go test ./internal/transaction -run '^$' -fuzz '^FuzzJournal$' -fuzztime 5s
          go test ./internal/transaction -run '^$' -fuzz '^FuzzClassification$' -fuzztime 5s
